import React from "react";
import "./Header.css";

const Header = () => {
    return (
        <header>
            <div>
                <div>
                    <button className="button-header">Log out</button>
                </div>
            </div>
        </header>
    );
};

export default Header;
